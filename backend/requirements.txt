# DeepTrace Backend — Phase 2 Dependencies
# ==========================================
# Phase 1: FastAPI server basics
# Phase 2: File upload support (python-multipart)
# Future phases will add: torch, opencv-python, numpy, Pillow, etc.

fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
